---
- name: Test conda_env module with conda
  hosts: localhost
  gather_facts: no
  tasks:
    - name: Set micromamba as conda_exe
      set_fact:
        conda_exe: "conda"
    - name: Include conda_env tests
      include_tasks: tasks/test_conda_env.yml
